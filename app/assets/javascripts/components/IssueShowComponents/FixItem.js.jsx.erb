var FixItem = React.createClass({
  render: function(){
    return (
      <div className="fix_item">
        <p><a href={"/issues/" + this.props.issue_id + "/fixes/" + this.props.id} >{this.props.title}</a></p>
      </div>
      )
  }
})
